apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: loki
  namespace: monitoring
spec:
  interval: 30m
  chart:
    spec:
      chart: loki
      version: "6.15.0"
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: loki
      interval: 12h

  valuesFrom:
    - kind: ConfigMap
      name: loki-values

  install:
    crds: Create
  upgrade:
    crds: CreateReplace

